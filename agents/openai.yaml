interface:
  display_name: "Transcribe Media"
  short_description: "Transcribe local and public media with captions first"
  default_prompt: "Use $transcribe-media to transcribe this local media file, podcast episode, or public media link, preferring existing captions."
